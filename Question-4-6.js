// Write an ES6 function that takes an array of objects and a property name and returns a new array with only the
//  values of that property. Avoid using in-built methods.



const getValues = (obj, property) =>{
    const{property,property,property} = obj;
    return [prp1,prp2,prp3];
}



console.log(
    getValues(
      [
        { name: "John", age: 21 },
        { name: "Mary", age: 22 },
        { name: "Peter", age: 23 },
      ],
      "name"
    )
  ); // ["John", "Mary", "Peter"]